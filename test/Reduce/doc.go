/*
Reduce applies a reducer function to each item emitted by an Observable and the
previous reducer result. Reduce emits only the final value.

The operator accepts a seed argument that is passed to the reducer for the
first item emitted by the ObservableInt.

	Reduce	http://reactivex.io/documentation/operators/reduce.html
*/
package Reduce
